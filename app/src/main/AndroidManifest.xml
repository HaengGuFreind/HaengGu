<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haenggu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name = ".data.api.GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_haenggu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_haenggu_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Haenggu">
        <activity android:name=".main.MainActivity"></activity>
        <activity android:name=".login.LoginActivity"></activity>
        <activity android:name=".event.EventActivity"></activity>
<!--        <activity android:name=".eventinfo.EventActivity"></activity>-->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>