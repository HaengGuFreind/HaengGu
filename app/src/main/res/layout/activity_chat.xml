<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".main.chat.ChatActivity">

    <EditText
        android:id="@+id/activity_chat_et"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/gray_box_50"
        android:fontFamily="@font/notosanskr_bold"
        android:foregroundGravity="bottom"
        android:textSize="15dp"
        android:gravity="left|center_vertical"
        android:includeFontPadding="false"
        android:maxLines="5"
        android:paddingVertical="9dp"
        android:paddingStart="19dp"
        android:paddingEnd="44dp"
        android:textFontWeight="400"
        android:hint="메세지 보내기"
        android:layout_marginHorizontal="15dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/activity_chat_btn"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center_vertical"
        android:layout_marginEnd="16dp"
        android:background="@mipmap/ic_launcher"
        android:enabled="false"
        app:layout_constraintBottom_toBottomOf="@+id/activity_chat_et"
        app:layout_constraintEnd_toEndOf="@+id/activity_chat_et"
        app:layout_constraintTop_toTopOf="@+id/activity_chat_et" />

</androidx.constraintlayout.widget.ConstraintLayout>